# Copy to .env.local (git-ignored) and fill in from your Supabase project
# settings → API. Same two values also go into Vercel's project env vars.
#
# No service role key needed for Phase 1 — see db/README.md.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
